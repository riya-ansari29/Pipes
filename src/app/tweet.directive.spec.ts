import { TweetDirective } from './tweet.directive';

describe('TweetDirective', () => {
  it('should create an instance', () => {
    const directive = new TweetDirective();
    expect(directive).toBeTruthy();
  });
});
